# =============================================================================
# Industrial Automation Suite - Konfiguration
# =============================================================================
# Kopieren Sie diese Datei nach .env und passen Sie die Werte an.
# WICHTIG: Generieren Sie sichere Passwörter und Secrets für Produktionsumgebungen!
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Konfiguration
# -----------------------------------------------------------------------------
POSTGRES_USER=automation
POSTGRES_PASSWORD=CHANGE_ME_secure_password_here
POSTGRES_DB=automation
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# NocoDB Konfiguration
# -----------------------------------------------------------------------------
# JWT Secret für Authentifizierung (min. 32 Zeichen empfohlen)
# Generieren mit: openssl rand -hex 32
NOCODB_JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32
NOCODB_PORT=8080
NOCODB_PUBLIC_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# n8n Konfiguration
# -----------------------------------------------------------------------------
# Encryption Key für Credentials (min. 32 Zeichen)
# Generieren mit: openssl rand -hex 32
N8N_ENCRYPTION_KEY=CHANGE_ME_generate_with_openssl_rand_hex_32
N8N_PORT=5678
N8N_HOST=localhost
N8N_PROTOCOL=http
N8N_WEBHOOK_URL=http://localhost:5678

# -----------------------------------------------------------------------------
# Metabase Konfiguration
# -----------------------------------------------------------------------------
# Secret Key für Verschlüsselung (min. 32 Zeichen)
# Generieren mit: openssl rand -hex 32
METABASE_SECRET_KEY=CHANGE_ME_generate_with_openssl_rand_hex_32
METABASE_PORT=3000

# -----------------------------------------------------------------------------
# Uptime Kuma Konfiguration
# -----------------------------------------------------------------------------
UPTIME_KUMA_PORT=3001

# -----------------------------------------------------------------------------
# Allgemeine Einstellungen
# -----------------------------------------------------------------------------
TIMEZONE=Europe/Berlin
